import { Box, Container, Grid, Button, Typography } from '@material-ui/core'

function Register(){
  return(
    <Box className="Register" flexGrow={1}>
      <Container fixed>
        <Grid container spacing={6} justify="center">

        </Grid>
      </Container>
    </Box>
  )
}

export default Register